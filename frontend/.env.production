# Native (Capacitor) builds need an absolute API URL; web builds ignore this
# and use same-origin /api via the vercel.json rewrite (see services/api.js).
REACT_APP_API_URL=https://backend-production-76f0e.up.railway.app/api
