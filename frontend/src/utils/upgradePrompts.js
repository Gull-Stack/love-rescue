/**
 * Upgrade Prompts — DISABLED
 *
 * The app is now fully free. Upgrade prompts have been removed.
 * These stubs are kept for import compatibility only.
 */

export const UPGRADE_TRIGGERS = {};

export const UPGRADE_COPY = {};

export function checkPostAssessmentPrompt(_completedCount) {
  return null;
}

export function getUpgradeCopy(_trigger) {
  return null;
}
